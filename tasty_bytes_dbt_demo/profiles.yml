tasty_bytes:
  target: dev
  outputs:
    dev:
      type: snowflake
      account: 'not needed'
      user: 'not needed'
      role: sysadmin
      database: tasty_bytes_dbt_db
      schema: dev
      warehouse: M_WAREHOUSE
    prod:
      type: snowflake
      account: 'not needed'
      user: 'not needed'
      role: sysadmin
      database: tasty_bytes_dbt_db
      schema: prod
      warehouse: M_WAREHOUSE
